# AssemblyAI API key — SOLO server-side. Copia a .env y nunca al repo.
# Se usa en api/token.js para emitir tokens temporales de un solo uso.
# Sin esta variable el sistema corre en modo mock (ver docs/architecture.md §9).
ASSEMBLYAI_API_KEY=
